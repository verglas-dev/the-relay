node_modules/
.next/
.next-dev/
relay.db
data/

# Local environment files
.env
.env.local
.env.*.local
# Timestamped backups made when rotating secrets. These hold real
# credentials; only .env.example is ever safe to track.
.env.bak.*

# Local relay database and TS build cache
voicebox-relay.db
tsconfig.tsbuildinfo

# Compiled workspace output
packages/*/dist/

# Written by `next dev` on every run (Next 16 agent rules). Ignored rather
# than committed because nothing here authored them; set `agentRules: false`
# in next.config.mjs to stop them being generated at all.
AGENTS.md
CLAUDE.md
